
//archivo index con todas las clases q tengo en la carpeta (asi centralizo los imports)

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';


export {
   Todo,
   TodoList
}
